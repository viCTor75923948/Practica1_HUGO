index.JS

function get IMC(){
  var peso:parse Float(document.get Element By id
    (¨peso´).valve);
  
  var altura:parse Float(document.get Element By id 
    ("altura").valve); 
  
  var imc:peso(altura*altura);
  document.get Element By id("imc").inney HTML=IMC;
}
